# Amber Terminal

**Dark theme** · id `amber-terminal` · file `amber-terminal.json`

> Warm amber monochrome inspired by vintage CRT terminals and early command-line workstations.

A single-hue theme: everything from body copy to borders is a step on one amber ramp over near-black, the way a monochrome phosphor tube only ever had one colour to give. IBM Plex Mono in both slots keeps the illusion intact — there is no proportional face anywhere in the UI. Best for long dark-room sessions where you want the interface to disappear behind the text.

## Preview

### 1. Dashboard

### 2. Explore

### 3. Modules

### 4. Papers

### 5. Paper Sample

### 6. Notes

### 7. Videos

## Palette

| Role | Token | Hex |
| --- | --- | --- |
| Page background | `bgcolor` | `#130E05` |
| Card / surface | `surfacecolor` | `#201708` |
| Surface border | `surfacebordercolor` | `#594017` |
| Primary | `primarycolor` | `#FFB627` |
| Primary (hover) | `primaryhovercolor` | `#FFC957` |
| Accent | `accentcolor` | `#FFD166` |
| Accent (soft) | `accentsoftcolor` | `#FFE3A3` |
| Foreground | `foregroundcolor` | `#FFD88A` |
| Body text | `fontcolor` | `#FFD88A` |
| Text (hover) | `fonthovercolor` | `#FFF0C2` |
| Muted text | `mutedcolor` | `#A58B59` |
| Icon pattern | `patterncolor` | `#FFE3A3` |

## Type

| Slot | Family | Source |
| --- | --- | --- |
| `sans` | IBM Plex Mono | Google Fonts |
| `display` | IBM Plex Mono | Google Fonts |

Fallback stack — sans: `ui-monospace, SFMono-Regular, Consolas, monospace`; display: `ui-monospace, SFMono-Regular, Consolas, monospace`.

## Want to try it?

Copy the contents of `amber-terminal.json` into StudyHub's **Custom Themes** editor — open the
Themes page, hit **Add custom theme**, paste the JSON, and save. Custom themes are
stored on your account, so they follow you to any device you sign in on, and you
get three slots to keep your favourites in.
